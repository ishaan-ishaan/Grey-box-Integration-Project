{
  "name": "grey-box",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npm run build:gas",
    "build:gas": "node esbuild.gas.mjs",
    "push": "npm run build:gas && clasp push --force",
    "clean": "rimraf build"
  },
  "devDependencies": {
    "@types/google-apps-script": "^2.0.7",
    "esbuild": "^0.27.0",
    "glob": "^10.3.10",
    "rimraf": "^5.0.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "@google/clasp": "^3.1.1"
  }
}
